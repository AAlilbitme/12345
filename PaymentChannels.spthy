/*
 * Standalone single-channel theory.
 * All rules, restrictions and base lemmas live in PaymentChannels.splib so
 * that layered theories (e.g. PaymentChannelMultihop.spthy) can reuse them
 * via #include without fact-arity clashes.
 */
theory PaymentChannel
begin

builtins: hashing, signing

#include "PaymentChannels.splib"

end
