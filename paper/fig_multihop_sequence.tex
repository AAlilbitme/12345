\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,calc,arrows.meta}
\usepackage{amsmath}
\usepackage{amssymb}

\definecolor{titledark}{RGB}{26,35,50}
\definecolor{lifeline}{RGB}{60,60,70}
\definecolor{honestgreen}{RGB}{27,94,32}
\definecolor{timeoutamber}{RGB}{230,126,20}
\definecolor{attackred}{RGB}{192,57,43}
\definecolor{bandgreen}{RGB}{223,237,222}
\definecolor{railgray}{RGB}{120,120,120}

\begin{document}
\begin{tikzpicture}[
  x=1cm, y=1cm,
  lifeline/.style={draw=lifeline, densely dotted, thick},
  headbox/.style={draw=none, fill=titledark, rounded corners=2pt,
                   minimum width=1.9cm, minimum height=0.75cm,
                   font=\bfseries\small, text=white},
  badge/.style={circle, draw=none, fill=#1, minimum size=0.55cm,
                font=\bfseries\scriptsize, text=white, inner sep=0pt},
  msg/.style={-{Latex[length=2.2mm]}, thick},
  msglab/.style={font=\scriptsize, align=center},
  legtitle/.style={font=\bfseries\scriptsize},
  legbody/.style={font=\scriptsize, align=left},
  roundband/.style={fill=bandgreen, draw=none}
]

% Fixed lifeline x-coordinates -- every message uses these directly, at a
% shared y per row, so arrows are always horizontal.
\def\xA{1.0}  % Alice
\def\xB{5.0}  % Bob
\def\xC{9.0}  % Carol
\def\xD{13.0} % Dave

% -------------------------------------------------------------------------
% Title (own row) + color key (row below, so they never collide)
% -------------------------------------------------------------------------
\node[anchor=west, font=\bfseries\large, titledark] at (0,14.4)
  {Multi-hop payment: Alice $\to$ Bob $\to$ Carol $\to$ Dave};

\node[badge=honestgreen] at (0.3,13.75) {};
\node[anchor=west, font=\scriptsize] at (0.6,13.75) {honest success / safety proved};
\node[badge=timeoutamber] at (5.6,13.75) {};
\node[anchor=west, font=\scriptsize] at (5.9,13.75) {timeout / refund};
\node[badge=attackred] at (8.6,13.75) {};
\node[anchor=west, font=\scriptsize] at (8.9,13.75) {attack (reachable)};

\node[anchor=west, font=\scriptsize, black!55] at (0,13.2)
  {annotated with \texttt{multihop.spthy} lemmas --- see rail $\to$};

% -------------------------------------------------------------------------
% Lifeline headers
% -------------------------------------------------------------------------
\node[headbox] (Alice) at (\xA,12.2) {Alice};
\node[headbox] (Bob)   at (\xB,12.2) {Bob};
\node[headbox] (Carol) at (\xC,12.2) {Carol};
\node[headbox] (Dave)  at (\xD,12.2) {Dave};

\draw[lifeline] (\xA,11.85) -- (\xA,-1.4);
\draw[lifeline] (\xB,11.85) -- (\xB,-1.4);
\draw[lifeline] (\xC,11.85) -- (\xC,-1.4);
\draw[lifeline] (\xD,11.85) -- (\xD,-1.4);

% -------------------------------------------------------------------------
% Invoice message (spans the whole width, drawn above round 1)
% -------------------------------------------------------------------------
\node[badge=honestgreen] at (-2.3,11.3) {0};
\draw[msg, honestgreen, densely dashed] (\xD,11.3) -- (\xA,11.3)
  node[msglab, midway, above] {Dave samples $x$, sends invoice $y=H(x)$ to Alice};

% -------------------------------------------------------------------------
% Round 1: LOCK
% -------------------------------------------------------------------------
\fill[roundband] (-3.1,7.55) rectangle (-2.6,10.75);
\node[rotate=90, font=\bfseries\scriptsize, titledark] at (-2.85,9.15) {ROUND 1 $\cdot$ LOCK};

\node[badge=honestgreen] at (-3.6,10.4) {1};
\draw[msg, honestgreen] (\xA,10.0) -- (\xB,10.0)
  node[msglab, midway, above] {HTLC(A$\to$B, 5, $y$, 3$t$)};

\node[badge=honestgreen] at (-3.6,9.15) {2};
\draw[msg, honestgreen] (\xB,9.0) -- (\xC,9.0)
  node[msglab, midway, above] {HTLC(B$\to$C, 5, $y$, 2$t$)};

\draw[msg, honestgreen] (\xC,8.0) -- (\xD,8.0)
  node[msglab, midway, above] {HTLC(C$\to$D, 5, $y$, $t$)};
\node[badge=honestgreen] at (14.4,8.0) {3};

% -------------------------------------------------------------------------
% Round 2: RELEASE
% -------------------------------------------------------------------------
\fill[roundband] (-3.1,4.55) rectangle (-2.6,7.35);
\node[rotate=90, font=\bfseries\scriptsize, titledark] at (-2.85,5.95) {ROUND 2 $\cdot$ RELEASE};

\node[badge=honestgreen] at (-3.6,7.0) {4};
\draw[msg, honestgreen] (\xD,7.0) -- (\xC,7.0)
  node[msglab, midway, above] {redeem($x$) --- Dave reveals $x$};

\draw[msg, honestgreen] (\xC,6.0) -- (\xB,6.0)
  node[msglab, midway, above] {redeem($x$)};

\draw[msg, honestgreen] (\xB,5.0) -- (\xA,5.0)
  node[msglab, midway, above] {redeem($x$)};

\node[badge=honestgreen] at (-3.6,4.85) {5};
\node[font=\bfseries\scriptsize, honestgreen, anchor=west] at (\xA,4.85)
  {$\to$ payment successful: each party paid, fee to each intermediary};

% -------------------------------------------------------------------------
% What can go wrong
% -------------------------------------------------------------------------
\node[anchor=west, font=\bfseries\scriptsize, black!70] at (-3.1,4.05) {WHAT CAN GO WRONG};
\draw[black!25] (-3.1,3.85) -- (14.4,3.85);

\node[badge=timeoutamber] at (-3.6,2.7) {6};
\draw[msg, timeoutamber, densely dashed] (\xB,2.7) -- (\xA,2.7)
  node[msglab, midway, above, timeoutamber, text width=7.5cm]
  {if $x$ never arrives: each HTLC refunds after its timeout. Staggering $t<2t<3t$ guarantees every intermediary redeems inbound before its outbound expires.};

\node[badge=attackred] at (-3.6,1.05) {7};
\draw[msg, attackred, densely dashed] (\xC,1.05) -- (\xA,1.05)
  node[msglab, midway, above, attackred, text width=7.5cm]
  {wormhole: Alice \& Carol collude, relay $x$ off-path $\to$ Bob is skipped, forwards value but earns no fee};

% -------------------------------------------------------------------------
% Staggered timelocks rail
% -------------------------------------------------------------------------
\node[badge=honestgreen] at (-3.6,0.2) {8};
\node[anchor=west, font=\bfseries\scriptsize, black!70] at (-3.1,0.2) {STAGGERED TIMELOCKS (block height $\to$)};

\fill[bandgreen] (\xA,-0.9) rectangle (\xD,-0.65);
\fill[bandgreen] (\xA,-1.15) rectangle (\xC,-0.9);
\fill[bandgreen] (\xA,-1.4) rectangle (\xB,-1.15);

\draw[railgray, thick, -{Latex}] (\xA,-1.65) -- (14.8,-1.65)
  node[right, font=\scriptsize, yshift=1pt] {redeem windows\\(nested)};
\foreach \x/\lab in {\xB/t (C$\to$D), \xC/2t (B$\to$C), \xD/3t (A$\to$B)} {
  \draw[attackred, thick] (\x,-0.6) -- (\x,-1.95);
  \node[font=\scriptsize, below] at (\x,-2.0) {\lab};
}

% -------------------------------------------------------------------------
% Footer
% -------------------------------------------------------------------------
\node[anchor=west, font=\scriptsize, black!45] at (0,-3.0)
  {Rough draft --- \texttt{multihop.spthy} lemmas mapped to the 4-party flow. Next: \texttt{cltv\_blocks} / \texttt{Clock} / \texttt{timeout}.};

% ===========================================================================
% Legend panel (right side) -- generous vertical spacing so 2-line bodies
% never collide with the next item's badge.
% ===========================================================================
\draw[black!20] (16.3,15.4) -- (16.3,-2.4);

\node[anchor=west, font=\bfseries\scriptsize, titledark] at (16.7,15.0)
  {LEMMAS $\cdot$ \texttt{multihop.spthy}};

\newcommand{\legitem}[5]{%
  \node[badge=#2] at (17.1,#1) {#3};
  \node[legtitle, anchor=west, text=#2] at (17.5,#1) {#4};
  \node[legbody, anchor=north west, text width=6.8cm] at (16.7,#1-0.35) {#5};
}

\legitem{14.4}{honestgreen}{1}{Invoice \& preimage authentication}
  {\texttt{Invoice\_Authenticates\_Settlement}, \texttt{Preimage\_Secret\_Until\_Released},\\
   \texttt{Forged\_Invoice\_Requires\_Key\_Compromise}, \texttt{Invoice\_Released\_Once} \checkmark}

\legitem{12.4}{honestgreen}{2}{Lock causality (forward needs previous)}
  {\texttt{Forward1\_Requires\_Offer}, \texttt{Forward2\_Requires\_Forward1},\\
   \texttt{HTLC\_On\_Opened\_Channel} \checkmark}

\legitem{10.7}{honestgreen}{3}{Release causality (redeem needs preimage)}
  {\texttt{Fulfill\_Requires\_Forward2}, \texttt{Claim\_Requires\_Release},\\
   \texttt{Settle\_Requires\_Receiver\_Release} \checkmark}

\legitem{9.0}{honestgreen}{4}{Atomicity + non-vacuity}
  {\texttt{Payment\_Atomicity\_Under\_Liveness} \checkmark,\\
   \texttt{Multihop\_Payment\_Possible} \checkmark\ (witness)}

\legitem{7.3}{honestgreen}{5}{Value \& fee conservation}
  {\texttt{EndToEnd\_Value\_Conservation}, \texttt{Fee\_Conservation\_Hop1/2},\\
   \texttt{Fee\_Strictly\_Positive\_Hop1/2}, \texttt{Receiver\_Paid\_Invoice\_Amount} \checkmark}

\legitem{5.3}{timeoutamber}{6}{Refund / griefing}
  {\texttt{Refund\_Requires\_Timeout}, \texttt{Loss\_Requires\_Inaction} \checkmark,\\
   \texttt{Honest\_Intermediary\_Refunds} \checkmark\ (reachable)}

\legitem{3.6}{attackred}{7}{Wormhole fee theft}
  {\texttt{Wormhole\_Fee\_Theft\_Reachable} \checkmark,\\
   \texttt{Wormhole\_Steals\_Exactly\_The\_Fees} \checkmark\ (theft = path fees)}

\legitem{1.9}{honestgreen}{8}{Intermediary safety (under liveness T2b)}
  {\texttt{Intermediary\_Never\_Loses\_Under\_Liveness} \checkmark,\\
   \texttt{Timeout\_Race\_Blocked} \checkmark\ (race blocked with T2b)}

\node[anchor=north west, font=\scriptsize, black!55, text width=6.8cm] at (16.7,0.0)
  {\checkmark\ = machine-checked in \texttt{multihop.spthy} (43/43).\\
   green = all-traces safety, orange = benign/reachable,\\
   red = attack shown reachable (by design, no PTLC).};

\node[anchor=north west, font=\scriptsize\itshape, black!45, text width=6.8cm] at (16.7,-1.6)
  {Channel-layer lemmas (open/update/close/punish)\\
   live on the Alice$\leftrightarrow$Bob figure.};

\end{tikzpicture}
\end{document}
